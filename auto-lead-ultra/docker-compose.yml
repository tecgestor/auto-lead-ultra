version: "3"

services:

 api:
  build: ./apps/api
  ports:
   - "4000:4000"

 web:
  build: ./apps/web
  ports:
   - "3000:3000"

 postgres:
  image: postgres
  environment:
   POSTGRES_PASSWORD: admin

 redis:
  image: redis
